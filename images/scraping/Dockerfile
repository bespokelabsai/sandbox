# Web scraping stack (requests, httpx, beautifulsoup4, lxml, parsel, scrapy).
FROM python:3.11-slim

RUN pip install --no-cache-dir \
    requests \
    httpx \
    beautifulsoup4 \
    lxml \
    parsel \
    scrapy
